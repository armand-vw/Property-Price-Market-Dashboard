# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/

# Generated data (reproducible from code)
data/*.csv
data/cache/

# NOTE: the fitted model (models/price_model.joblib) intentionally IS committed
# so the hosted app loads instantly instead of retraining on every cold start.
